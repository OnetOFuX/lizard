name: lizard-obs

services:
  prometheus:
    image: prom/prometheus:v3.5.0
    container_name: lizard-prometheus
    restart: unless-stopped
    ports:
      - "29090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - lizard_prometheus_data:/prometheus
    networks:
      - lizard-prod-net

  loki:
    image: grafana/loki:3.4.1
    container_name: lizard-loki
    restart: unless-stopped
    ports:
      - "23100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - ./loki/config.yml:/etc/loki/local-config.yaml:ro
      - lizard_loki_data:/loki
    networks:
      - lizard-prod-net

  promtail:
    image: grafana/promtail:3.4.1
    container_name: lizard-promtail
    restart: unless-stopped
    command: -config.file=/etc/promtail/config.yml
    volumes:
      - ./promtail/config.yml:/etc/promtail/config.yml:ro
      - ../infra/gateway/logs:/var/log/ms/gateway:ro
      - ../services/catalogo-ms/logs:/var/log/ms/catalogo-ms:ro
      - ../services/producto-ms/logs:/var/log/ms/producto-ms:ro
      - ../services/orden-ms/logs:/var/log/ms/orden-ms:ro
      - ../services/pago-ms/logs:/var/log/ms/pago-ms:ro
      - ../services/carrito-ms/logs:/var/log/ms/carrito-ms:ro
      - ../services/auth-ms/logs:/var/log/ms/auth-ms:ro
    depends_on:
      - loki
    networks:
      - lizard-prod-net

  grafana:
    image: grafana/grafana:11.6.0
    container_name: lizard-grafana
    restart: unless-stopped
    ports:
      - "23000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - lizard_grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
      - loki
    networks:
      - lizard-prod-net

volumes:
  lizard_prometheus_data:
  lizard_loki_data:
  lizard_grafana_data:

networks:
  lizard-prod-net:
    external: true
    name: lizard-prod-net
